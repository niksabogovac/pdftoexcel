namespace Common.Enums
{
    public enum DeleteActionType 
    {
        // QR codes from file.
        QRCode = 1,
        // Box from file.
        Box = 2,
        // Single input QR code.
        QRCodeSingle = 3,
    }
}
